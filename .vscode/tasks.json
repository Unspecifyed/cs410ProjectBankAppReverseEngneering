{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build Program",
            "type": "shell",
            "command": "clang++",
            "args": [
                "-std=c++17",
                "-g",
                "${workspaceFolder}/*.cpp",
                "-o",
                "${workspaceFolder}/output",
                "-I",
                "${workspaceFolder}",
                "-L",
                "/lib/x86_64-linux-gnu",
                "-lpthread",
                "-lssl",
                "-lcrypto"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
        {
            "label": "build and run Test",
            "type": "shell",
            "command": "bash",
            "args": [
                "-c",
                "clang++ -std=c++17 -g ${workspaceFolder}/test/test.cpp ${workspaceFolder}/*.cpp -o ${workspaceFolder}/test/test_output -I ${workspaceFolder} -L /lib/x86_64-linux-gnu -lgtest -lgtest_main -lpthread -lssl -lcrypto && ${workspaceFolder}/test/test_output"
            ],
            "group": {
                "kind": "test",
                "isDefault": true
            }
        }
    ]
}
